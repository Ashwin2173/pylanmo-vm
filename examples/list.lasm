NATIVE print
NATIVE input

main {
	PUSH print
        PUSH len
            PUSH hello
            CALL 0
        CALL 1
	CALL 1
	PUSH 0
	RETURN
}

hello {
	PUSH print
	PUSH "Hello, World"
	CALL 1
	PUSH "hello"
	RETURN
}
