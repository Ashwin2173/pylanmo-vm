NATIVE print

main {
    PUSH 3
    PUSH 2
    PUSH 1
    MAKE_LIST 3    // [3, 2, 1]
    STORE list

    LOAD list
    PUSH 100
    SET_INDEX 0
    STORE list

    PUSH print
    LOAD list
    CALL 1
    PUSH NONE
    RETURN
}